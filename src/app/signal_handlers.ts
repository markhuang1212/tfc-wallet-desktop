import { ipcMain } from 'electron'

ipcMain.handle('get-accounts', async (event, arg) => {
    
})

ipcMain.handle('add-account', async (event, accountInfo) => {

})

ipcMain.handle('remove-account', async (event, accountId) => {

})

ipcMain.handle('transfer-coin', async (async, accountId) => {

})